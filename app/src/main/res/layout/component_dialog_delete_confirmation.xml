<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="330dp"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/component_dialog_background">

    <LinearLayout
        android:id="@+id/dialog_delete_main_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/dialog_delete_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Excluir Lista"
            android:textSize="26sp"
            android:textColor="@color/softBlack"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            style="@style/TextAppearance.App.Heading1"/>

        <TextView
            android:id="@+id/dialog_delete_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tem certeza que deseja excluir essa lista? Esta ação não poderá ser desfeita."
            android:textColor="@color/softBlack"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/dialog_delete_title"
            app:layout_constraintStart_toStartOf="parent"
            style="@style/TextAppearance.App.Paragraph"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="24dp"
            app:layout_constraintTop_toBottomOf="@id/dialog_delete_message"
            android:layout_gravity="center_horizontal">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/dialog_delete_cancel_button"
                android:layout_width="120dp"
                android:layout_height="60dp"
                android:text="Cancelar"
                android:layout_marginEnd="32dp"
                style="@style/Widget.App.Button.Secondary"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/dialog_delete_delete_button"
                android:layout_width="120dp"
                android:layout_height="60dp"
                android:text="Excluir"
                style="@style/Widget.App.Button.Delete"/>
        </LinearLayout>
    </LinearLayout>

    <View
        android:id="@+id/dialog_delete_bottom_line"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:background="@drawable/component_dialog_bottom_line"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>